import type { BoardEdge, BoardNode } from './types';

/** The board starts empty — users build their own model from the palette. */
export const initialNodes: BoardNode[] = [];

export const initialEdges: BoardEdge[] = [];
